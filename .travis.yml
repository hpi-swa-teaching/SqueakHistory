language: smalltalk
sudo: false

before_script:
  - pwd
#  - ln -s ./history -t $SMALLTALK_CI_BUILD/
  - ls -ltrA

after_script:
  - pwd
  - ls -ltrA
# Select operating system(s)
os:
  - linux
  - osx

# Select compatible Smalltalk image(s)
smalltalk:
  - Squeak-trunk
  - Squeak-5.0
  - Squeak-4.6
  - Squeak-4.5
