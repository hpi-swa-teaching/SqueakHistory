SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'SqueakHistory',	//Define MC Baseline
      #directory : 'packages',		//Path to packages if FileTree is used
      #load : 'Tests',				//Define MC load attributes
      #platforms : [ #squeak ]		//Define compativle platforms
    }
  ]
}